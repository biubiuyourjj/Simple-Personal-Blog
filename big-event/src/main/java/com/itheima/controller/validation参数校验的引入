1.引入依赖
2.参数前添加@Pattern注解
3.在Controller类上添加@Validated注解

@Validated 是 Spring 对 JSR-303/JSR-380（Java 规范的参数校验）的增强实现，主要作用是：
触发对控制器方法、服务层方法入参的校验规则（比如字段非空、长度限制、格式校验等）。
支持分组校验（不同场景用不同的校验规则，比如新增和修改用户时校验规则不同）。
可以标注在类、方法、参数上，最常用在控制器类 / 方法或参数上