Md5Util 是一个MD5 加密工具类，核心作用是将字符串（如密码、文件内容等）转换成固定 32 位的
MD5 哈希值，常用于密码存储、数据完整性校验（比如验证文件是否被篡改）等场景。

JwtUtil 是基于auth0版JWT框架封装的令牌生成与解析工具类，核心作用是：
生成包含业务数据的JWT Token（用于接口鉴权，比如用户登录后返回令牌）；
验证Token的合法性（签名、有效期）并解析出其中的业务数据（比如接口请求时校验令牌并获取用户信息）。